## ScoredDefaultProfile.gd
## Default scored AI profile — balanced play with default weights.
## Suitable as a baseline for both player and enemy AI.
class_name ScoredDefaultProfile
extends ScoredCombatProfile

## Default weights — no overrides needed for balanced play.
func _init() -> void:
	pass
